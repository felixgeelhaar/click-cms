{
    "name": "click/cms",
    "description": "A lightweight headless CMS with a full plugin system",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": ">=8.1"
    },
    "autoload": {
        "psr-4": {
            "Click\\Cms\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Click\\Cms\\Tests\\": "tests/"
        }
    },
    "config": {
        "sort-packages": true
    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "require-dev": {
        "phpunit/phpunit": "^13.2"
    },
    "scripts": {
        "test": "phpunit --no-progress",
        "test:admin": "cd admin-ui && npm test",
        "lint": "find src plugins bin -name '*.php' -print0 | xargs -0 -n1 php -l > /dev/null",
        "seed": "php bin/click-seed.php"
    },
    "scripts-descriptions": {
        "test": "Run the PHP test suite.",
        "test:admin": "Run the admin UI test suite.",
        "lint": "Syntax-check every PHP file in src, plugins and bin.",
        "seed": "Fill a fresh install with the example site. Never overwrites."
    }
}
